{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Windows Launch",
            "program": "${workspaceFolder}/Poly.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceRoot}"
        }
    ]
}