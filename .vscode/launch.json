{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Windows Launch",
            "program": "${workspaceFolder}/Polya.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceRoot}"
        }
    ]
}